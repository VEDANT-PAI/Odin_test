# Odin frontend

The Odin frontend is a Next.js book discovery experience. It connects to the FastAPI service through `NEXT_PUBLIC_API_URL` and falls back to a small demo catalog while the local database is being initialized.

```bash
npm install
npm run dev
```

Open http://localhost:3000.

